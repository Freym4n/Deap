<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Noticias"
            android:label="Noticias">

        </activity>
        <activity
            android:name=".detalleCurso"
            android:label="Curso" />
        <activity
            android:name=".PerfilProfe"
            android:label="@string/opciones" />
        <activity
            android:name=".IndexCursos"
            android:label="@string/title_activity_index_cursos" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>